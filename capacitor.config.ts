import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vole.app',
  appName: 'Vole Chauffeur',
  webDir: 'dist/vole',
  bundledWebRuntime: false
};

export default config;
